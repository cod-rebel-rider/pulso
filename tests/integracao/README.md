# tests/integracao

Testes de integração (módulos + persistência combinados). Serão escritos a partir da **Fase 02 — Banco de Dados**, quando a camada de persistência existir.

Regra do projeto: nenhum teste é criado antecipadamente para funcionalidades que ainda não existem.
